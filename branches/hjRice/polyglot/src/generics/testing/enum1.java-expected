class nested {
    public enum SortableProperty {
        CATALOG_ID (  ),
        
        NAME (  ),
        
        WIDTH (  ),
        
        DEPTH (  ),
        
        HEIGHT (  ),
        
        MOVABLE (  ),
        
        ;
        
        
        
        
        private SortableProperty() {  }
    }
    
    
    private boolean enumError(nested.SortableProperty p) {
        switch (p) {
            case NAME:
            case WIDTH: return true; default: return false; }
    }
    
    public nested() { super(); }
}

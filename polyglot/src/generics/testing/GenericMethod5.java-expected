import java.util.*;

class test {
    public static <V extends java.lang.Comparable<V>> V max(V a, V b) {
        if (a.compareTo(b) > 0) return a;
        return b; }
    
    public test() { super(); }
}


interface Drawable {}

interface Comparable<T> {
    int comp(T t);
}

interface mytype<T extends Drawable & Comparable<S>> {
    public void m(T input) ;
}

class C implements mytype {
   public void m(Comparable input){
    }
}

